{
  "name": "cv-analyzer-api",
  "version": "1.0.0",
  "description": "API for CV analysis and job application assistance.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:generate": "npx prisma generate --schema ../prisma/schema.prisma"
  },
  "keywords": [
    "cv",
    "resume",
    "ats",
    "job-application"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^7.0.1",
    "dotenv": "^16.0.3",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "prisma": "^7.0.1"
  }
}
