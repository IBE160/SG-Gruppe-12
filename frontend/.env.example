# =============================================================================
# Frontend Environment Configuration (Next.js)
# =============================================================================
# This file contains all environment variables required for the frontend app.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API base URL
# Development: http://localhost:3001/api/v1
# Production: Your Render backend URL (e.g., https://your-app.onrender.com/api/v1)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3001/api/v1"

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Application environment
NEXT_PUBLIC_APP_ENV="development"  # Options: development | production

# Application name (displayed in UI)
NEXT_PUBLIC_APP_NAME="AI CV Assistant"

# -----------------------------------------------------------------------------
# Feature Flags (Optional)
# -----------------------------------------------------------------------------
# Enable/disable features for development or A/B testing
# NEXT_PUBLIC_ENABLE_ANALYTICS=false
# NEXT_PUBLIC_ENABLE_CV_VERSIONING=true
# NEXT_PUBLIC_ENABLE_COVER_LETTER_GEN=true

# -----------------------------------------------------------------------------
# Analytics & Monitoring (Optional)
# -----------------------------------------------------------------------------
# Google Analytics
# NEXT_PUBLIC_GA_TRACKING_ID=""

# Sentry (Frontend error tracking)
# NEXT_PUBLIC_SENTRY_DSN=""

# -----------------------------------------------------------------------------
# Production Deployment Checklist (Vercel)
# -----------------------------------------------------------------------------
# Before deploying to Vercel, ensure:
# ✓ NEXT_PUBLIC_API_BASE_URL points to your Render backend
# ✓ NEXT_PUBLIC_APP_ENV is set to "production"
# ✓ All API keys are added to Vercel environment variables
# ✓ Build command is configured: npm run build
# ✓ Output directory is set to: .next
# -----------------------------------------------------------------------------
